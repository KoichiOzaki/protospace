.col-sm-4.col-md-3.proto-content
  .thumbnail
    = link_to "prototypes/#{ prototype.id }" do
      = prototype.prototype_images.each do |image|
        - if image.pr_flag == 1 then
          = image_tag(image.image)
          - break
      .caption
        %h3
        = prototype.title
        .proto-meta
          .proto-posted
            = prototype.created_at.strftime("%b - %d")
        %ul.proto-tag-list.list-inline
          %li 
            = link_to "タグ機能coming soon", "", class: "btn btn-default"
          %li 
            = link_to "タグ機能coming soon", "", class: "btn btn-default"
